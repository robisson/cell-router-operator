---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cellrouters.cell.cellrouter.io
spec:
  group: cell.cellrouter.io
  names:
    kind: CellRouter
    listKind: CellRouterList
    plural: cellrouters
    shortNames:
    - cellrtr
    singular: cellrouter
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CellRouter represents the routing layer that exposes cell entrypoints.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of CellRouter.
            properties:
              gateway:
                description: Gateway defines the Gateway resource that exposes the
                  cell entrypoints.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are merged into the Gateway annotations.
                    type: object
                  gatewayClassName:
                    description: GatewayClassName references the GatewayClass to use.
                    minLength: 1
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels are merged into the Gateway labels.
                    type: object
                  listeners:
                    description: Listeners configures the listeners exposed by the
                      Gateway.
                    items:
                      description: CellGatewayListener represents a single gateway
                        listener.
                      properties:
                        hostname:
                          description: Hostname restricts the listener to a specific
                            hostname when provided.
                          maxLength: 253
                          minLength: 1
                          pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                        name:
                          description: Name uniquely identifies the listener within
                            the Gateway.
                          minLength: 1
                          type: string
                        port:
                          description: Port is the network port exposed by the listener.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          description: Protocol defines the protocol supported by
                            the listener.
                          maxLength: 255
                          minLength: 1
                          pattern: ^[a-zA-Z0-9]([-a-zA-Z0-9]*[a-zA-Z0-9])?$|[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/[A-Za-z0-9]+$
                          type: string
                        tls:
                          description: TLS configures TLS settings for the listener
                            when the protocol requires it.
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - name
                      - port
                      - protocol
                      type: object
                    minItems: 1
                    type: array
                  name:
                    description: Name is the name of the managed Gateway.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the namespace where the Gateway is created.
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - gatewayClassName
                - listeners
                - name
                - namespace
                type: object
              routes:
                description: Routes describes how traffic should be routed to cell
                  entrypoints.
                items:
                  description: CellRouteSpec describes an HTTPRoute managed by the
                    operator.
                  properties:
                    additionalBackends:
                      description: |-
                        AdditionalBackends expands the route into a weighted multi-backend rule.
                        Each referenced Cell must be traffic-ready before it is included.
                      items:
                        description: CellRouteBackendRef points to a Cell and optional
                          weight within a generated HTTPRoute rule.
                        properties:
                          cellRef:
                            description: CellRef points to the target Cell.
                            minLength: 1
                            type: string
                          weight:
                            description: Weight controls the relative traffic weight
                              towards the referenced Cell.
                            format: int32
                            type: integer
                        required:
                        - cellRef
                        type: object
                      type: array
                    cellRef:
                      description: CellRef points to the target Cell that receives
                        the traffic.
                      minLength: 1
                      type: string
                    fallbackBackend:
                      description: FallbackBackend is used when no primary backend
                        for the route is traffic-ready.
                      properties:
                        cellRef:
                          description: CellRef points to the target Cell.
                          minLength: 1
                          type: string
                        weight:
                          description: Weight controls the relative traffic weight
                            towards the referenced Cell.
                          format: int32
                          type: integer
                      required:
                      - cellRef
                      type: object
                    headerMatches:
                      description: HeaderMatches constrains traffic using HTTP header
                        matching.
                      items:
                        description: HTTPHeaderMatch represents a single HTTP header
                          match rule.
                        properties:
                          name:
                            description: Name is the HTTP header name to match.
                            maxLength: 256
                            minLength: 1
                            pattern: ^[A-Za-z0-9!#$%&'*+\-.^_\x60|~]+$
                            type: string
                          type:
                            description: Type selects the header match strategy. Defaults
                              to HeaderMatchExact.
                            enum:
                            - Exact
                            - RegularExpression
                            type: string
                          value:
                            description: Value is the HTTP header value to match.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    hostnames:
                      description: Hostnames declares the hostnames handled by this
                        route.
                      items:
                        description: |-
                          Hostname is the fully qualified domain name of a network host. This matches
                          the RFC 1123 definition of a hostname with 2 notable exceptions:

                           1. IPs are not allowed.
                           2. A hostname may be prefixed with a wildcard label (`*.`). The wildcard
                              label must appear by itself as the first label.

                          Hostname can be "precise" which is a domain name without the terminating
                          dot of a network host (e.g. "foo.example.com") or "wildcard", which is a
                          domain name prefixed with a single wildcard label (e.g. `*.example.com`).

                          Note that as per RFC1035 and RFC1123, a *label* must consist of lower case
                          alphanumeric characters or '-', and must start and end with an alphanumeric
                          character. No other punctuation is allowed.
                        maxLength: 253
                        minLength: 1
                        pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                      type: array
                    listenerNames:
                      description: ListenerNames restricts the route to one or more
                        listeners. When empty, all listeners apply.
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is the name of the HTTPRoute resource generated
                        for this rule.
                      minLength: 1
                      type: string
                    pathMatch:
                      description: PathMatch constrains traffic to a specific path
                        expression.
                      properties:
                        type:
                          description: Type is the path matching strategy. Defaults
                            to PathMatchPathPrefix.
                          enum:
                          - Exact
                          - PathPrefix
                          - RegularExpression
                          type: string
                        value:
                          description: Value is the path value.
                          minLength: 1
                          type: string
                      required:
                      - value
                      type: object
                    queryParamMatches:
                      description: QueryParamMatches constrains traffic using HTTP
                        query parameter matching.
                      items:
                        description: HTTPQueryParamMatch represents a single query
                          parameter match rule.
                        properties:
                          name:
                            description: Name is the query parameter name.
                            maxLength: 256
                            minLength: 1
                            pattern: ^[A-Za-z0-9!#$%&'*+\-.^_\x60|~]+$
                            type: string
                          type:
                            description: Type selects the match strategy. Defaults
                              to QueryParamMatchExact.
                            enum:
                            - Exact
                            - RegularExpression
                            type: string
                          value:
                            description: Value is the expected parameter value.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    weight:
                      description: Weight controls the relative traffic weight towards
                        the cell.
                      format: int32
                      type: integer
                  required:
                  - cellRef
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - gateway
            - routes
            type: object
          status:
            description: Status defines the observed state of CellRouter.
            properties:
              conditions:
                description: Conditions represent the current state of the CellRouter
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              managedGatewayRef:
                description: ManagedGatewayRef stores the namespace/name of the managed
                  gateway.
                type: string
              managedRoutes:
                description: ManagedRoutes contains the managed route summary.
                items:
                  description: ManagedRouteStatus reports the status of a generated
                    HTTPRoute.
                  properties:
                    backendRefs:
                      description: BackendRefs lists the backend cells currently materialized
                        for the route.
                      items:
                        type: string
                      type: array
                    cellRef:
                      description: CellRef is the cell the route points to.
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the route condition
                        changed.
                      format: date-time
                      type: string
                    listenerNames:
                      description: ListenerNames lists the listeners associated with
                        the route.
                      items:
                        type: string
                      type: array
                    name:
                      description: Name identifies the HTTPRoute.
                      type: string
                    placementRef:
                      description: PlacementRef identifies the CellPlacement that
                        produced the route when applicable.
                      type: string
                    reason:
                      description: Reason summarizes why the route is ready or waiting.
                      type: string
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration tracks the last reconciled generation.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
