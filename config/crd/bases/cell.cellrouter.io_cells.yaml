---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: cells.cell.cellrouter.io
spec:
  group: cell.cellrouter.io
  names:
    kind: Cell
    listKind: CellList
    plural: cells
    shortNames:
    - cell
    singular: cell
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cell is the Schema for the cells API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Cell
            properties:
              entrypoint:
                description: Entrypoint configures the Service that exposes workloads
                  inside the cell.
                properties:
                  port:
                    description: Port specifies the port exposed by the Service.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    description: Protocol defines the Service port protocol. Defaults
                      to TCP when omitted.
                    type: string
                  serviceName:
                    description: ServiceName defines the name of the Service created
                      inside the cell namespace.
                    minLength: 1
                    type: string
                  targetPort:
                    description: |-
                      TargetPort overrides the port targeted on the selected pods.
                      Defaults to the same value as Port when omitted.
                    format: int32
                    type: integer
                  type:
                    description: Type controls the Service type. Defaults to ClusterIP
                      when omitted.
                    type: string
                required:
                - port
                - serviceName
                type: object
              namespace:
                description: |-
                  Namespace overrides the namespace name generated for the cell.
                  When omitted, the namespace defaults to the Cell's name.
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              namespaceAnnotations:
                additionalProperties:
                  type: string
                description: |-
                  NamespaceAnnotations are merged into the managed Namespace annotations.
                  Keys managed by the operator cannot be overridden.
                type: object
              namespaceLabels:
                additionalProperties:
                  type: string
                description: |-
                  NamespaceLabels are merged into the managed Namespace labels.
                  Keys managed by the operator cannot be overridden.
                type: object
              serviceAnnotations:
                additionalProperties:
                  type: string
                description: ServiceAnnotations are merged into the managed Service
                  annotations.
                type: object
              serviceLabels:
                additionalProperties:
                  type: string
                description: ServiceLabels are merged into the managed Service labels.
                type: object
              tearDownOnDelete:
                description: |-
                  TearDownOnDelete controls whether the operator should delete the managed namespace
                  once the Cell resource is removed. The default is false to protect workloads.
                type: boolean
              workloadSelector:
                additionalProperties:
                  type: string
                description: WorkloadSelector selects the Pods that back the managed
                  Service.
                type: object
            required:
            - entrypoint
            type: object
          status:
            description: status defines the observed state of Cell
            properties:
              conditions:
                description: Conditions represent the current state of the Cell resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              entrypointService:
                description: EntrypointService holds the fully qualified name (namespace/name)
                  of the managed Service.
                type: string
              namespace:
                description: Namespace reflects the effective namespace name managed
                  for the cell.
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the last reconciled generation.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
